/* converta valores numéricos para string, calcule quantos dígitos há em 2 elevado a um milhão. */

let numero = 2**1000000
let contagem = numero + ''
alert("Existe "+contagem.length+" Digitos")
